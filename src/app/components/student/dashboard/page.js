export default function StudentHome() {
  return (
    <div>
      <h2 className="text-2xl font-bold">Dashboard</h2>
      <p className="mt-2 text-gray-600">Student dashboard summary, quick stats, etc.</p>
      {/* cards, upcoming assignments, progress bar ইত্যাদি এখানে যোগ করবেন */}
    </div>
  );
}
